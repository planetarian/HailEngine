using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Graupel
{
    /*
    public class PositionSpan
    {
        private Parser parser;
        private Position start;

        public PositionSpan(Parser parser, Position start)
        {
            this.parser = parser;
            this.start = start;
        }

        public Position End
        {
            get { return start.Union(parser.Last(1).Position); }
        }
    }*/
}
